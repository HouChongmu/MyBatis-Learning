<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yolyn.chapter031.mapper.TaskMapper">
    <select id="getTask" parameterType="long" resultType="com.yolyn.chapter031.pojo.Task">
        select id, title, context, note from t_task_ssm where id = #{id}
    </select>
</mapper>